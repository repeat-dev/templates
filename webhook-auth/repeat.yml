featured: true
category: web

repeat:
  name: Webhook with auth
  description: A webhook template that checks x-webhook-secret header

events:
  - type: webhook

variables:
  - key: WEBHOOK_SECRET
    value: replace-me
    is_secret: true
